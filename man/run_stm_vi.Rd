% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Rfunction.R
\name{run_stm_vi}
\alias{run_stm_vi}
\title{Supervised topic model (STM) variational inference with ELBO-based convergence.}
\usage{
run_stm_vi(
  count,
  y,
  K,
  alpha,
  beta,
  phi = NULL,
  seed = NULL,
  max_iter = 200L,
  min_iter = 50L,
  tol_elbo = 1e-04,
  update_sigma = TRUE,
  tau = 20L,
  show_progress = TRUE,
  chunk = 5000L,
  verbose = TRUE,
  sigma2_init = NULL
)
}
\arguments{
\item{count}{Integer matrix with 3 columns (d, v, c) in 0-based indexing.
Each row represents document index \code{d}, word index \code{v}, and
token count \code{c}.}

\item{y}{Numeric vector of length D. Must not contain NA values.}

\item{K}{Integer, number of topics. Required if \code{phi} is NULL;
ignored if \code{phi} is provided (then \code{K = ncol(phi)}).}

\item{alpha}{Dirichlet prior parameter for document–topic distributions.}

\item{beta}{Dirichlet prior parameter for topic–word distributions.}

\item{phi}{Optional V × K topic–word probability matrix used only for
initializing topic assignments.}

\item{seed}{Optional integer random seed used in the initialization step.}

\item{max_iter}{Maximum number of variational sweeps.}

\item{min_iter}{Minimum number of sweeps before checking ELBO convergence.}

\item{tol_elbo}{Numeric tolerance for relative ELBO change.}

\item{update_sigma}{Logical; if TRUE, update \code{sigma2} each sweep.}

\item{tau}{Numeric log-space cutoff used in \code{stm_vi_parallel()}.}

\item{show_progress}{Logical; print low-level progress inside C++.}

\item{chunk}{Integer; number of documents per parallel block.}

\item{verbose}{Logical; print ELBO and relative change per sweep.}

\item{sigma2_init}{Optional numeric scalar specifying the initial
noise variance. If NULL, \code{sigma2} is estimated once by least squares.}
}
\value{
A list containing:
  \describe{
    \item{nd}{D × K document–topic count matrix.}
    \item{nw}{K × V topic–word count matrix.}
    \item{ndsum}{Length-D vector of document token counts.}
    \item{nwsum}{Length-K vector of topic token counts.}
    \item{eta}{K-dimensional regression coefficient vector.}
    \item{sigma2}{Final noise variance.}
    \item{phi}{V × K topic–word posterior mean.}
    \item{theta}{D × K document–topic posterior mean.}
    \item{elbo}{Final ELBO.}
    \item{label_loglik}{Final supervised term.}
    \item{elbo_trace}{ELBO values per sweep.}
    \item{label_loglik_trace}{Label log-likelihood per sweep.}
    \item{n_iter}{Number of iterations actually performed.}
    \item{D, V, K, NZ}{Model dimensions.}
  }
}
\description{
This function performs supervised topic model (STM) using variational inference.
It initializes topic assignments from \code{count} (optionally using a
topic–word prior \code{phi}), estimates regression parameters, and repeatedly
calls the C++ routine \code{stm_vi_parallel()} until convergence.
}
\details{
Convergence is assessed based on the relative changes in the evidence lower
bound (ELBO) and the supervised label log-likelihood:
  \deqn{
    \frac{\mathrm{ELBO}_t - \mathrm{ELBO}_{t-1}}{|\mathrm{ELBO}_{t-1}|},
    \qquad
    \frac{\ell_t - \ell_{t-1}}{|\ell_{t-1}|}.
  }
After a minimum number of iterations, the algorithm is declared to have
converged when both quantities are non-negative and smaller than the
prescribed tolerance.

**Important:**
This function assumes that the response vector \code{y} contains **no NA**
values. The underlying C++ implementation does not skip missing responses
and requires \code{y[d]} to be finite for all documents.
}
